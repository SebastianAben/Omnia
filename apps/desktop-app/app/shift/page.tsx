import { AppShell } from "@/components/app-shell";
import { ShiftPanel } from "@/features/shift/shift-panel";

export default function ShiftPage() {
  return (
    <AppShell>
      <ShiftPanel />
    </AppShell>
  );
}
